<span class="absolute flex gap-2 px-2 py-1 text-sm text-white bg-gray-700 rounded-md right-4 top-4">
  <%= button_to '+', add_to_cart_path(sneaker), class:'font-bold text-xs pb-1' %>
  <span class="flex items-center font-light text-gray-600">|<span class="w-5 mx-1 font-bold text-center text-white"><%= @cart.sneaker_count(sneaker)%></span>|</span>
  <%= button_to '-', remove_from_cart_path(sneaker), method: :delete, class:'font-black text-xs pb-1' %>
</span>
